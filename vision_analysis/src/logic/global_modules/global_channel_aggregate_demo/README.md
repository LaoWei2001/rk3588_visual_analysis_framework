# 多通道聚合教学示例

通道 Logic 用 `publish_*()` 公开变量；本模块遍历 `gctx->inputs()` 读取业务输入。
框架会自动选择画布连入通道（没有连线时使用应用全部通道），并排除尚未发布、离线或
长时间没有更新的输入。业务代码不需要判断数据年龄。

核心代码只有三步：遍历通道、组合变量、调用 `report_event()`。需要固定通道时可直接使用
`gctx->input(channel_id)`；该通道当前不可用时返回 `nullptr`。
