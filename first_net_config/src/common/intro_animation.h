#ifndef FIRST_NET_CONFIG_INTRO_ANIMATION_H
#define FIRST_NET_CONFIG_INTRO_ANIMATION_H

/* Play the optional ncurses startup animation. Returns immediately when the
 * terminal UI has not been initialized or the terminal is too small. */
void intro_animation_play(void);

#endif
