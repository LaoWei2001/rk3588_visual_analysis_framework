# 开发日志（备忘录）

> 文档角色：历史归档，不是当前开发入口、接口说明或基线清单。请从 [docs 文档总入口](README.md) 开始，并以现行源码和对应专题文档为准。  
> 本文件按时间保留历史决策；每个日期条目中的“当前”都只表示该日期当时的状态。早期的 Redis、直接 uploader API、旧 logic 名称、旧源码路径和外部 ROI 文件等描述不得直接用于现行开发。

## 2025.12 - 2026.3

编程语言的选择：c/c++。因为底层的硬件加速依赖库只提供c++的接口，且python本身就不适合硬件性能受限的情况，多路推理远远达不到要求，使用python初步尝试多路视频推理后发现4路流连10帧都没有。还尝试过c++取流，python进行推理的方式，性能也很差。

模型结构的选择：模型输入端始终为单batch，最开始想设计为deepstream那样的多batch输入，但是性能不好,还是固定采用单batch，多线程的设计。

解决了诸多问题：如何训练出符合要求的自定义的pt模型，如何转onnx，转rknn，如何使用自己训练的模型进行图片推理。

确认程序的基本架构：各模块低耦合设计，底层解码，缩放等流程尽量与上层逻辑分离，便于二次开发。依赖配置文件设置以自由搭配各路视频的逻辑，模型，输入源类型。

## 2025.3 - 2025.4

支持平台远程下发并热更新模型文件，支持各种参数的实时更新，代码结构模块化，标准化以便于二次开发。

不断优化性能，从一开始最多支持流畅（25帧）推理2-3路视频，提高到了8路左右。

在15路并发的状态下推理帧数仍能达到每秒11-12帧，接近英伟达的jetson orin nano上使用deepstream框架推理的性能（16路13帧左右）满足简单场景的需求。

支持将视频流手动绑定至某一个npu的核心，npu有3个核心，建议总的视频流个数为3的倍数，可以使npu负载较均衡。

上报模块从主程序中解耦为 Python 微服务程序。此阶段曾采用 Redis 报警队列；该实现后来已被
`event_store` 文件事件发件箱替代，不能作为当前开发接口。

## 2026.04.23

将程序一部分专用的库替换为通用的库，程序能够在其他厂商的RK3588上运行，操作系统尽量是debian, 没有在ubuntu等其他系统上试过。

## 2026.04.28

去掉RGA全局互斥锁，成功调用两个RGA核心的资源，最后一个RGA核心不要用，总是卡死。

让显示与推理线程分离，改善显示效果，缺点是会出现检测框跟不上快速移动的目标的效果。

摄像头分辨率建议设置720p及以下，因为视频帧尺寸会被压缩到640x640再送入模型，所以我认为原始分辨率高了没什么用，反而影响传输和解码速度。

## 2026.04.29

优化全局逻辑函数调用各个通道变量时的时间一致性。

## 2026.04.30

制作docker镜像，现已支持在98服务器上进行docker交叉编译，生成dist文件夹，但是感觉交叉编译没必要，不方便测试实际运行效果。

## 2026.05.06

调试半天最后发现程序在另一台RK3588上性能不佳的原因居然是网络问题，那一台不能用5Gwifi。

修复RGA缩放导致本地视频显示有绿条的问题。若要使视频文件循环播放，要在配置文件里面写"loop": true， 不是"loop": 1。

视频文件分辨率尽量为1920*1080，1280*720。

## 2026.05.07

去掉正式部署时后台程序保存日志文件的功能，防止长时间运行会导致root文件夹空间不足的情况。

build.sh现可以自动判断平台类型(arm/x86)进行不同的编译方式，交叉编译只支持在98服务器上，因为只有这上面预置了镜像。

编译后直接生成正式部署的文件夹，内置一键部署/关闭服务的功能，可随时使用journalctl -u xxxx -f 查看后台程序的实时终端输出。

若运行deploy.sh脚本遇到了部署失败的情况，需要查看实时终端输出排查具体原因，若提示Permission denied，需要chmod +x xxx.sh给予脚本权限。

## 2026.05.08

发现在vscode中第一次启动新编译的程序时，连接到RK3588的显示屏常常不出现视频画面。(在板端输入指令启动程序显示画面之后即可恢复), 原因未知。

下一步优化dify工作流触发的设计: 配置文件里面配置有dify上报逻辑的可以直接输入提示词，然后大模型直接根据提示词输出0/1，在工作流中进行后续操作，提示词不再写死在dify应用中，达到一种人工控制某个通道逻辑的效果，不需要写代码重新编译，也便于后续修改功能。

这样如果不同通道使用的逻辑不一样也可以共用一个dify工作流。

当时计划为服务器/Dify 的 Redis 入队和消费补单元测试；当前测试目标应改为 `report_event → manifest/media → EventOutboxForwarder`，不再测试 Redis 队列。

## 2026.05.11

将通道逻辑和全局逻辑的相关代码文件移出analyzer文件夹, 放进单独的logic文件夹, 核心理念是:二次开发时不必关注底层的实现, 只需关注logic文件夹的内容就行了

增加暂停功能, 选中显示画面的窗口, 用空格键可以控制程序的暂停/开始, 便于调试

## 2026.05.12

之前发现了一个问题, 检测框总是滞后于目标的移动, 现在显示效果有些提高。

## 2026.05.14

现在绘制到视频画面上的自定义图形(圆,文字等)会随着视频窗口的缩放而变化。

## 2026.05.15

如果多个通道共用同一个逻辑，逻辑内部需要跨帧保存的变量必须使用 `ctx->state` 维护，否则不同通道的数据会混在一起。当前可对照 `logic_upload` 的闩锁和 `logic_button_demo` 的动作状态。

错误例子:

```cpp
static void logic_xxx(ChannelContext *ctx)
{
    static uint64_t last_ts = 0; // 所有通道共享，互相覆盖
    ...
}
```

正确例子:

```cpp
struct XxxState
{
    uint64_t last_ts = 0;
    bool first = true;
};
static void logic_xxx(ChannelContext *ctx)
{
    if (!ctx->state)
        ctx->state = std::make_shared<XxxState>();
    auto &s = *std::static_pointer_cast<XxxState>(ctx->state);

    // 直接用 s.last_ts、s.first，每个通道就使用的是独立的变量
}
```

## 2026.05.18

将多线程库更换为pthread，在main.cpp中可清晰看见各个线程的创建。

## 2026.05.26

目前程序的各模块解耦设计的风格十分适合开发一个低代码平台。但是目前我认为只适合简单的危险区域入侵

如果低代码平台要适配复杂一点的算法比如说货架的那个项目目前比较困难，还没想到好方法。怎么设计是个大问题。

## 2026.05.27

我觉得低代码有两个层次:低一点的层次本质上是一个可视化的配置工具, 配置视频源,模型,上报方式这些基本的参数以及调用开发者预先写好的算法逻辑，能够方便使用者配置参数，部署程序（粗粒度）。

高层次就支持算法编排都可以在画布上完成（细粒度）, 比如dify上可针对输入的图片利用LLM节点以及代码执行节点就可以进行自由的后续算法编排

这样的效果对于视觉分析这样定制性较高的项目来说, 完成难度极大，且大概率还是无法完成有新需求的任务，因为dify能完成相关任务我认为核心在于LLM的输出可以用自然语言定制，然后使用代码节点进行处理。

但是就目前这个多路视频分析的项目来说，实在想不到好的方法来设计代码节点然后对yolo模型输出的结果直接进行后处理（我没有办法用自然语言告诉yolo，让他输出定制化的结果），举例子：我可以在dify上让LLM直接输出图中有几个人，有没有戴安全帽，然后直接拿到结果数字做后续的算法编排。但是yolo只会输出框在哪里，类别是什么，这些都需要设计代码节点来完成，且我无法想象定制性类似那种货架区域入侵的项目怎么样直接拖拖拽拽模块就能实现。

而且就算能实现代码节点的设计，那面对一个新场景的时候也得在代码节点里面写大段的编排代码，跟我直接在代码文件里面写没有区别，而且用户使用这个低代码工具的时候也不会自己写代码。

所以我将这样低代码拖拽模块的效果以一个配置工具而不是算法编排工具来呈现。

以前新部署一个程序需要手动写json配置文件，现在可以拖拖拽拽模块然后自动生成json配置文件，我觉得也还可以，至少比之前给黄经理那种配置参数的程序强

而且现在是多路视频流，以这种配置的方式来呈现也更清晰。

## 2026.06.01

为配置平台添加了RTSP实时显示功能, 想观看运行效果可以不用连接显示屏了

为画布增加了复制/粘贴,撤销/恢复的快捷键功能

让一些设置变得更人性化

让后端代码自动读取c++主程序中的参数(放入了logics.json中), 然后自动更新ui界面中的参数配置项

修复bug:我发现当我第一次打开程序的时候, cpu占用率50%,然后我点击查看画面, cpu占用率增加了20%,我关掉显示的窗口,然后再打开窗口, 又增加了20%左右,直到这样重复开关到cpu占用率几乎要到100%了, 画面就全黑了,程序也无法停止了, 这时我刷新网页, cpu就掉回50%

原因是后端拉流没随关窗释放的资源泄漏。

TODO:后续需要测试关于添加, 修改参数的这一整套流程。还有将service文件夹中的两个微服务的参数看看能不能统一起来用这个web配置界面管理（一个模型更新的服务，一个上报服务）

然后再全面测试，看所有模块能不能正常工作，再想想系统的架构是否可以简化。

## 2026.06.03

发现将usb视频源作为输入节点, 然后在ROI配置区域设定好ROI区域之后, 将底部的全局设置的最大FPS设为15, 运行时画面中的ROI没有偏差, 但是我改成25之后, 画面上的ROI就偏离了本来的位置,且视野变大了

原因是USB摄像头的帧率和采集分辨率是绑定的, 高的FPS会绑定低分辨率，此时如果在该分辨率的画布上画ROI区域用到其他分辨率上就会产生偏移。

发现问题：长时间连续15路高npu负载运行约20小时之后，cpu的负载会剧烈波动(20%~70%)，推理帧数也会剧烈波动(5~13)，且重启设备仍未改善，关闭程序一段时间后能恢复正常。

具体原因未知，很有可能是cpu温度过高触发操作系统的保护机制。

## 2026.06.04

在web界面上加入终端功能，最多支持同时开启4个终端窗口。
完善skill文件夹，下一阶段是人工对着代码和md文档审核+测试+完善文档，最终目的是如果来一个新需求就可以直接给大模型这个文件夹，按照里面的说明直接生成代码。

最好是让人也看得懂，跟着md文档也能自己一步一步完成，这就说明md文档足够全面，到时候就算给傻乎乎的大模型（豆包之类的）如果也能顺利完成需求，那就好了。

目前skill包括了通道逻辑和关于调试的内容,下一步再添加全局逻辑的内容。

## 2026.06.05

继续完善skills文件夹

规定json配置文件里面每一种视频源在配置的时候必须写上视频源类型

## 2026.06.12

将通道逻辑(channel_logic)做了拆分重构: 原来所有 logic_xxx 函数都挤在 channel_logic.cpp 一个文件里(2600+行), 当时改为每个逻辑独立成一个 `src/logic/logic_xxx.cpp` 文件。此处是 2026.06.12 的历史路径；现行结构已经继续演进为 `src/logic/modules/<logic_name>/{logic.cpp,logic.json}`，框架核心位于 `src/logic/core/`。

注册方式从「在 channel_logic_init() 里手动 register_logic」改成「文件末尾一行 REGISTER_LOGIC(...) 自注册」, 在 main() 之前的静态初始化阶段自动登记到分发表。好处: 新增一个逻辑=新增一个文件, 删除一个逻辑=删除对应文件, 互不牵连, 耦合最低。src/logic 下的 .cpp 由 CMake(aux_source_directory)自动收集编译, build.sh 每次全新构建会自动包含, 无需手动改 CMake。

注意: channel_logic_init()/deinit() 改成了空实现(仅保留以兼容 analyzer 的调用), 绝不能再在里面清零/清空注册表, 否则会抹掉静态期已完成的自注册, 导致重启后所有通道退化为空逻辑。

当时曾同步过 channel logic 文档并补充多 ROI 说明，也保留过一批随后从生产源码删除的业务示例。2026.07.14 又按当时四个实际 logic 清理过示例目录；本条只记录该阶段的演进状态，不是现行模块清单。

将整个代码文件夹已上传至github仓库

## 2026.07.14（历史快照，已被后续模块化更新取代）

> 以下内容只描述 2026.07.14 当日状态。当前模块与服务边界请从 [docs 文档总入口](README.md) 查询。

统一告警链路已经收敛为
`report_event → event_store/<event_id>/{event.json,media_state.json,delivery_state.json} + 媒体 → unified_upload → adapter`。C++ 不维护网络协议，连接地址和密钥集中保存在上传服务 `config.yaml` 的 Profile 中，Web 通道配置只保存 `report_policy`、`report_parameters` 和 delivery 的 mapping。

截至 2026.07.14，当时生产 `src/logic/` 的具体实现为 `logic_default`、`logic_upload`、`logic_button_demo` 和 `logic_path_sop`。当日删除了文档中对已不存在的 `logic_server`、`logic_dify`、`logic_hook` 等示例的现行引用，并按当时源码补齐四篇示例。后续又增加了正式模块，因此这四项不能作为现行清单。ROI 在该阶段从通道内嵌配置加载并支持热更新；通道数量、顺序或 id 的拓扑变化仍要求重启。

全局逻辑当前没有接受 `GlobalContext*` 的统一媒体告警入口。跨通道规则若需图片/视频告警，应由明确的锚点通道 logic 提交，或先新增线程安全的公共 API；不能伪造 `ChannelContext`。

## 2026.07.16

通道 logic 命名收敛为单一真源：`REGISTER_LOGIC` 从两参数改为 `REGISTER_LOGIC(logic_func)`，宏直接把 C++ 入口函数名字符串化。该名称同时供运行时注册表、`channels[].logic`、Web 和外部 API 使用，不再维护手写注册串。

模块源 `logic.json` 不再声明 `name`；构建生成器从 `REGISTER_LOGIC(logic_func)` 提取函数名并注入生成的 App `logics.json`。模块目录只用于组织源码，可以与 logic ID 不同；每个模块仍必须有 `logic.json`。`REGISTER_LOGIC_ACTION` 的第一参数也改为 logic 入口函数标识符，由宏自动生成同一 logic ID。

#########################

项目移至github进行版本管理
